# 以太坊数据集测试指令

---

## 数据集信息

| 属性     | eth.dat (完整)     | eth_small.dat (推荐) | eth_medium.dat |
| -------- | ------------------ | -------------------- | -------------- |
| 交易数   | 679,999            | 3,400                | 34,000         |
| 区块数   | 20,000             | 100                  | 1,000          |
| Gas 范围 | 21,000 - 8,000,000 | 同左                 | 同左           |

---

## 步骤 0: 创建测试数据集

在项目目录下执行（PowerShell）：

```powershell
# 创建小型数据集（100个区块，3400条交易）
Get-Content D:\vchain-plus-master\data\dataset\eth.dat -Head 3400 | Set-Content D:\vchain-plus-master\data\dataset\eth_small.dat

# 创建中型数据集（1000个区块，34000条交易）
Get-Content D:\vchain-plus-master\data\dataset\eth.dat -Head 34000 | Set-Content D:\vchain-plus-master\data\dataset\eth_medium.dat
```

或使用 Git Bash / WSL：

```bash
head -3400 data/dataset/eth.dat > data/dataset/eth_small.dat
head -34000 data/dataset/eth.dat > data/dataset/eth_medium.dat
```

---

## 步骤 1: 生成公钥

### 推荐配置（q=4096，约需 512MB 内存）

`target\release\gen_key.exe -q 4096 -o D:\vchain-plus-master\output\pk_eth.key`

### 备选配置

| q 值  | 内存需求 | 适用场景 |
| ----- | -------- | -------- |
| 1024  | ~128MB   | 快速测试 |
| 4096  | ~512MB   | **推荐** |
| 8192  | ~1GB     | 中等规模 |
| 16384 | ~2GB     | 较大规模 |

---

## 步骤 2: 构建区块链数据库

### 使用以太坊数据集

`target\release\build_chain.exe -t 2 -t 4 -t 8 -t 16 -t 32 -t 64 --id-fanout 4 -b 4 -m 4095 -d 1 -k D:\vchain-plus-master\output\pk_eth.key -i D:\vchain-plus-master\data\dataset\eth.dat -r D:\vchain-plus-master\output\build_time_eth.json -o D:\vchain-plus-master\output\db_eth`

**参数说明：**
| 参数 | 值 | 说明 |
|------|-----|------|
| `-t` | 2,4,8,16,32 | 滑动窗口大小（升序） |
| `--id-fanout` | 4 | ID 树分支因子 |
| `-b` | 4 | B+树分支因子 |
| `-m` | 4095 | MaxID（必须 ≤ q-1） |
| `-d` | 1 | 数据维度（Gas 是 1 维） |

---

## 步骤 3: 创建查询 JSON 文件

### query_eth_1.json - 基础单地址查询

```json
[
    {
        "start_blk": 1,
        "end_blk": 50,
        "range": [[21000, 500000]],
        "keyword_exp": {
            "input": "'0x174bfa6600bf90c885c7c01c7031389ed1461ab9'"
        }
    }
]
```

### query_eth_2.json - AND 布尔查询

```json
[
    {
        "start_blk": 1,
        "end_blk": 100,
        "range": [[50000, 300000]],
        "keyword_exp": {
            "and": [
                {
                    "input": "'0x174bfa6600bf90c885c7c01c7031389ed1461ab9'"
                },
                {
                    "input": "'0x5c0f9c75c939d309d122f083d1b6450ba70b8644'"
                }
            ]
        }
    }
]
```

### query_eth_3.json - OR 布尔查询

```json
[
    {
        "start_blk": 1,
        "end_blk": 100,
        "range": [[21000, 1000000]],
        "keyword_exp": {
            "or": [
                {
                    "input": "'0x174bfa6600bf90c885c7c01c7031389ed1461ab9'"
                },
                {
                    "input": "'0x87c9ea70f72ad55a12bc6155a30e047cf2acd798'"
                }
            ]
        }
    }
]
```

### query_eth_4.json - 复杂嵌套查询

```json
[
    {
        "start_blk": 1,
        "end_blk": 100,
        "range": [[100000, 2000000]],
        "keyword_exp": {
            "and": [
                {
                    "or": [
                        {
                            "input": "'0x174bfa6600bf90c885c7c01c7031389ed1461ab9'"
                        },
                        {
                            "input": "'0x06012c8cf97bead5deae237070f9587f8e7a266d'"
                        }
                    ]
                },
                {
                    "input": "'0x52bc44d5378309ee2abf1539bf71de1b7d7be3b5'"
                }
            ]
        }
    }
]
```

### query_eth_batch.json - 批量测试

```json
[
    {
        "start_blk": 1,
        "end_blk": 20,
        "range": [[21000, 100000]],
        "keyword_exp": {
            "input": "'0x174bfa6600bf90c885c7c01c7031389ed1461ab9'"
        }
    },
    {
        "start_blk": 1,
        "end_blk": 50,
        "range": [[50000, 500000]],
        "keyword_exp": {
            "or": [
                { "input": "'0x174bfa6600bf90c885c7c01c7031389ed1461ab9'" },
                { "input": "'0x21ab6c9fac80c59d401b37cb43f81ea9dde7fe34'" }
            ]
        }
    },
    {
        "start_blk": 1,
        "end_blk": 100,
        "range": [[100000, 1000000]],
        "keyword_exp": {
            "and": [
                { "input": "'0x174bfa6600bf90c885c7c01c7031389ed1461ab9'" },
                { "input": "'0x5c0f9c75c939d309d122f083d1b6450ba70b8644'" }
            ]
        }
    }
]
```

---

## 步骤 4: 运行查询与验证

### 完整优化（推荐）

`target\release\query.exe -e -n -k D:\vchain-plus-master\output\pk_eth.key -i D:\vchain-plus-master\output\db_eth -q D:\vchain-plus-master\output\query_eth.json -r D:\vchain-plus-master\output\result_eth.json -v 12`

### 批量查询测试

```powershell
target\release\query.exe ^
    -e -n ^
    -k D:\vchain-plus-master\output\pk_eth.key ^
    -i D:\vchain-plus-master\output\db_eth ^
    -q D:\vchain-plus-master\output\query_eth_batch.json ^
    -r D:\vchain-plus-master\output\result_eth_batch.json ^
    -v 8
```

**参数说明：**
| 参数 | 说明 |
|------|------|
| `-e` | 启用查询计划优化 (egg optimizer) |
| `-n` | 启用空集剪枝优化 |
| `-v 8` | 使用 8 个验证线程 |

---

## 常用以太坊地址（用于查询）

数据集中高频地址：
| 地址 | 出现次数 |
|------|----------|
| `0x174bfa6600bf90c885c7c01c7031389ed1461ab9` | 306 |
| `0x87c9ea70f72ad55a12bc6155a30e047cf2acd798` | 136 |
| `0x06012c8cf97bead5deae237070f9587f8e7a266d` | 72 (CryptoKitties) |
| `0x52bc44d5378309ee2abf1539bf71de1b7d7be3b5` | 97 |
| `0x21ab6c9fac80c59d401b37cb43f81ea9dde7fe34` | 63 |

```
对比实验：
实验 1：链级证明大小对比（随链长变化）
实验目标：展示 MMR 证明大小随链长对数增长，而区块头链同步线性增长
实验设计：链长 n = [
    100, 200, 300, 400, 500, 750, 1000, 1500, 2000, 2500,
    3000, 4000, 5000, 6000, 7000, 8000, 9000, 10000,
    12000, 14000, 16000, 18000, 20000
]
实验 1 指令：cargo test --test experiment1_proof_size --release -- --nocapture
```

```
实验 2：链级区块头认证（随链长变化）
实验目标：展示 MMR 的验证时间随链长对数增长。
实验设计：链长 n = [
    100, 200, 300, 400, 500, 750, 1000, 1500, 2000, 2500,
    3000, 4000, 5000, 6000, 7000, 8000, 9000, 10000,
    12000, 14000, 16000, 18000, 20000
]
实验 2 指令：cargo test --test experiment2_verify_time --release -- --nocapture
```

新名称说明
Baseline-A HeaderChain 区块头链同步验证
Baseline-B Merkle Merkle树 证明验证
Baseline-C MMR-Scattered MMR + 分散索引根
Ours Two-Layer MMR + BlockADSRoot展开（两层式）

第四章实验：加入布隆过滤器实现否布尔查询优化加速：
实验一：用18年以太坊数据集（较密集）控制变量对比布隆过滤器以及非布隆过滤器

实验指令1（非bloom）：cargo run --release --bin build_chain -- -t 2 -t 4 -t 8 -t 16 -t 32 -t 64 --id-fanout 4 -b 4 -m 4095 -d 1 --key-path .\output\pk_eth.key --input .\data\dataset\eth.dat --output .\output\eth_chain_2018 --result .\output\eth_chain_2018\result.json
实验指令2（是bloom）：cargo run --release --bin build_chain -- -t 2 -t 4 -t 8 -t 16 -t 32 -t 64 --id-fanout 4 -b 4 -m 4095 -d 1 --enable-bloom --key-path .\output\pk_eth.key --input .\data\dataset\eth.dat --output .\output\eth_chain_bloom --result .\output\eth_chain_bloom\result.json
实验二：用24年以太坊数据集（较稀疏）控制变量对比布隆过滤器以及非布隆过滤器

后续步骤
Step 4 涉及查询计划改写、新 proof 类型，预计 200-300 行（已完成）
Step 5 涉及跨网络 proof 序列化、签名链、轻节点验证流程，预计 200+ 行（已完成）

实验步骤：
步骤1：
基于2024以太坊数据集生成新的公钥
`Remove-Item -Recurse -Force D:\vchain-plus-master\output\pk_eth2024.key -ErrorAction SilentlyContinue`
`D:\vchain-plus-master\target\release\gen_key.exe -q 4096 -o D:\vchain-plus-master\output\pk_eth2024.key`

步骤2：
基于2024以太坊数据集构建链
--Paper A 链
`Remove-Item -Recurse -Force D:\vchain-plus-master\output\db_eth2024_papera -ErrorAction SilentlyContinue`

`D:\vchain-plus-master\target\release\build_chain.exe -t 2 -t 4 --id-fanout 4 -b 4 -m 4095 -d 1 -k D:\vchain-plus-master\output\pk_eth2024.key -i D:\vchain-plus-master\data\dataset\eth_2024.dat -r D:\vchain-plus-master\output\build_time_eth2024_papera.json -o D:\vchain-plus-master\output\db_eth2024_papera`

--创新点3
`Remove-Item -Recurse -Force D:\vchain-plus-master\output\db_eth2024_schemex -ErrorAction SilentlyContinue`
`D:\vchain-plus-master\target\release\build_chain.exe -t 2 -t 4 --id-fanout 4 -b 4 -m 4095 -d 1 --enable-bloom -k D:\vchain-plus-master\output\pk_eth2024.key -i D:\vchain-plus-master\data\dataset\eth_2024.dat -r D:\vchain-plus-master\output\build_time_eth2024_schemex.json -o D:\vchain-plus-master\output\db_eth2024_schemex`
