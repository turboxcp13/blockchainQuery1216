阶段 1：MMR 基础模块测试（P0 - 核心依赖）

cargo test --lib chain::mmr::helper::tests
-- 通过

# 第 2 步：测试 BlockADSMerge 合并策略

cargo test --lib chain::mmr::block_ads_merge::tests
-- 通过

# 第 3 步：测试内存存储 MemStore

cargo test --lib chain::mmr::mmr_store::tests
-- 通过

# 第 4 步：测试 RocksStore 持久化存储

cargo test --lib chain::mmr::rocks_store::tests
-- 通过

# 第 5 步：测试 MMR 核心操作（push/root/proof）

cargo test --lib chain::mmr::mmr::tests
-- 通过

# 运行所有创新点 2 集成测试

cargo test --test two_layer_proof_integration_test
--通过
cargo test --test simchain_mmr_integration_test
--通过
cargo test --test build_block_mmr_test
--通过
cargo test --test innovation2_e2e_test

# 带详细输出运行

cargo test --test innovation2_e2e_test -- --nocapture

# 运行特定测试

cargo test --test innovation2_e2e_test test_full_workflow -- --nocapture

# 运行所有创新点 2 相关测试

cargo test mmr
cargo test two_layer
cargo test innovation2
