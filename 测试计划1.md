# 第 1 步：测试核心工具模块

cargo test --lib digest::tests
--通过
cargo test --lib utils::tests
--通过

# 第 2 步：测试累加器基础

cargo test --lib acc::set::tests
--通过
cargo test --lib acc::ops::tests
--通过

````
---

###  阶段 2：索引结构测试（P1 - 核心功能）

测试三种索引树的正确性，这是创新点 1 的数据来源。

```bash
# 第3步：测试索引树模块
cargo test --lib chain::id_tree::tests -- --test-threads=1
--通过
cargo test --lib chain::bplus_tree::tests
--通过
cargo test --lib chain::trie_tree::tests
--通过
# 第4步：测试累加器其他功能
cargo test --lib acc::
--通过
````

---

### 阶段 3：创新点 1 专项测试

```bash
# 第5步：BlockADSRoot 单元测试（7个）
cargo test --lib chain::block::block_ads_root::tests
--通过

# 第6步：BlockADSRoot 集成测试（8个）
cargo test --test block_ads_root_integration_test
--通过
# 带详细输出
cargo test --test block_ads_root_integration_test -- --nocapture
-- 通过
```

---

### 🟢 阶段 4：端到端测试（P2 - 集成验证）

```bash
# 第7步：区块链集成测试
cargo test --lib chain::tests
-- 通过
# 第8步：验证流程测试
cargo test --lib chain::verify::tests
-- 通过
# 第9步：查询流程测试
cargo test --lib chain::query::tests
-- 通过
```
