# 📋 测试计划与推进策略

## 一、当前测试状态概览

### 测试分布统计（共 72 个单元测试 + 8 个集成测试）

| 模块 | 文件 | 测试数量 | 优先级 |
|------|------|---------|--------|
| **核心工具** | `digest.rs` | 5 | 🔴 P0 |
| | `utils.rs` | 9 | 🔴 P0 |
| **累加器** | `acc/set.rs` | 5 | 🔴 P0 |
| | `acc/ops.rs` | 3 | 🔴 P0 |
| | `acc/poly.rs` | 2 | 🟡 P1 |
| | `acc/acc_value.rs` | 2 | 🟡 P1 |
| | `acc/utils.rs` | 3 | 🟡 P1 |
| | `acc/keys.rs` | 1 | 🟡 P1 |
| | `acc/serde_impl.rs` | 2 | 🟡 P1 |
| **索引树** | `id_tree/tests.rs` | 5 | 🟡 P1 |
| | `id_tree/write.rs` | 1 | 🟡 P1 |
| | `bplus_tree/tests.rs` | 5 | 🟡 P1 |
| | `trie_tree/tests.rs` | 6 | 🟡 P1 |
| **创新点1** | `block/block_ads_root.rs` | 7 | 🔴 P0 |
| **区块链** | `chain/tests.rs` | 2 | 🟢 P2 |
| | `verify.rs` | 1 | 🟢 P2 |
| **查询** | `query.rs` | 1 | 🟢 P2 |
| | `query/egg_qp.rs` | 1 | 🟢 P2 |
| | `query/query_param.rs` | 2 | 🟢 P2 |
| | `query/query_plan.rs` | 1 | 🟢 P2 |
| **集成测试** | `tests/block_ads_root_integration_test.rs` | 8 | 🔴 P0 |

---

## 二、测试推进策略（自底向上）

### 🔴 阶段1：基础层测试（P0 - 必须先通过）

这些是最底层的模块，其他所有功能都依赖它们。**必须确保100%通过**。

```bash
# 第1步：测试核心工具模块
cargo test --lib digest::tests
cargo test --lib utils::tests

# 第2步：测试累加器基础
cargo test --lib acc::set::tests
cargo test --lib acc::ops::tests
```

**通过标准**：所有基础测试必须 0 失败

---

### 🟡 阶段2：索引结构测试（P1 - 核心功能）

测试三种索引树的正确性，这是创新点1的数据来源。

```bash
# 第3步：测试索引树模块
cargo test --lib chain::id_tree::tests
cargo test --lib chain::bplus_tree::tests  
cargo test --lib chain::trie_tree::tests

# 第4步：测试累加器其他功能
cargo test --lib acc::
```

**通过标准**：所有索引树的插入、删除、证明生成测试通过

---

### 🔴 阶段3：创新点1专项测试（P0 - 论文核心）

**这是你论文的核心创新，必须重点测试！**

```bash
# 第5步：BlockADSRoot 单元测试（7个）
cargo test --lib chain::block::block_ads_root::tests

# 第6步：BlockADSRoot 集成测试（8个）
cargo test --test block_ads_root_integration_test

# 带详细输出
cargo test --test block_ads_root_integration_test -- --nocapture
```

**当前单元测试覆盖情况**：

| 测试名称 | 测试内容 | 状态 |
|---------|---------|------|
| `test_block_ads_root_creation` | 创建和基本验证 | ✅ 已有 |
| `test_block_ads_root_deterministic` | 确定性（相同输入=相同输出） | ✅ 已有 |
| `test_block_ads_root_different_components` | 不同组件产生不同根 | ✅ 已有 |
| `test_verify_components_failure` | 恶意组件检测 | ✅ 已有 |
| `test_digestible_trait` | Digestible trait | ✅ 已有 |
| `test_verify_self` | 内部一致性验证 | ✅ 已有 |
| `test_components_accessor` | 组件访问器 | ✅ 已有 |

**建议补充的单元测试**：

```rust
#[test]
fn test_new_method() {
    // 测试新增的 new() 方法
    let components = BlockADSComponents::new(...);
    let root_digest = components.compute_root();
    let ads_root = BlockADSRoot::new(root_digest, components.clone());
    assert!(ads_root.verify_self());
}

#[test]
fn test_from_digest_without_components() {
    // 测试轻节点场景：只有 root 没有 components
    let root = BlockADSRoot::from_digest(some_digest);
    assert!(!root.verify_self()); // 应该失败，因为 components 是默认值
}
```

---

### 🟢 阶段4：端到端测试（P2 - 集成验证）

```bash
# 第7步：区块链集成测试
cargo test --lib chain::tests

# 第8步：验证流程测试
cargo test --lib chain::verify::tests

# 第9步：查询流程测试
cargo test --lib chain::query::tests
```

---

## 三、推荐的测试命令

### 快速验证（每次修改后运行）
```bash
# 只测试创新点1相关
cargo test block_ads_root -- --nocapture
```

### 完整测试（提交前运行）
```bash
# 运行所有测试
cargo test

# 运行所有测试并显示输出
cargo test -- --nocapture
```

### 单个文件测试
```bash
# 语法：cargo test --lib 模块路径::tests
cargo test --lib chain::block::block_ads_root::tests
cargo test --lib acc::set::tests
```

### 集成测试
```bash
# 运行特定集成测试文件
cargo test --test block_ads_root_integration_test

# 运行所有集成测试
cargo test --test '*'
```

---

## 四、测试检查清单

### ✅ 创新点1 测试检查清单

- [ ] **单元测试（7个）**
  - [ ] `test_block_ads_root_creation` 通过
  - [ ] `test_block_ads_root_deterministic` 通过
  - [ ] `test_block_ads_root_different_components` 通过
  - [ ] `test_verify_components_failure` 通过
  - [ ] `test_digestible_trait` 通过
  - [ ] `test_verify_self` 通过
  - [ ] `test_components_accessor` 通过

- [ ] **集成测试（8个）**
  - [ ] `test_light_node_verification_flow` 通过
  - [ ] `test_block_ads_root_lifecycle` 通过
  - [ ] `test_malicious_components_detection` 通过
  - [ ] `test_multi_block_scenario` 通过
  - [ ] `test_deterministic_computation` 通过
  - [ ] `test_component_order_matters` 通过
  - [ ] `test_block_ads_root_creation_methods` 通过
  - [ ] `test_build_style_workflow` 通过

- [ ] **BlockHead.verify_ads_components() 方法**
  - [ ] 正确组件返回 true
  - [ ] 篡改组件返回 false

---

## 五、测试执行顺序（建议）

```
第1天：基础层 (P0)
   └── digest, utils, acc::set, acc::ops
   
第2天：创新点1 单元测试 (P0)
   └── block_ads_root.rs 中的 7 个测试
   
第3天：创新点1 集成测试 (P0)  
   └── block_ads_root_integration_test.rs 中的 8 个测试
   
第4天：索引树 (P1)
   └── id_tree, bplus_tree, trie_tree
   
第5天：端到端 (P2)
   └── chain::tests, verify, query
```

---

## 六、常见问题排查

### 测试失败时的排查步骤

1. **查看失败信息**
   ```bash
   cargo test 测试名 -- --nocapture 2>&1 | head -50
   ```

2. **单独运行失败的测试**
   ```bash
   cargo test --lib 模块::tests::失败的测试名 -- --nocapture
   ```

3. **检查依赖模块是否通过**
   - 如果 `block_ads_root` 测试失败，先确保 `digest` 测试通过

4. **查看编译错误**
   ```bash
   cargo check 2>&1 | head -30
   ```

---

## 七、测试覆盖率（可选）

如果需要查看测试覆盖率：

```bash
# 安装 tarpaulin
cargo install cargo-tarpaulin

# 生成覆盖率报告
cargo tarpaulin --out Html

# 查看报告
open tarpaulin-report.html
```

**目标覆盖率**：
- 创新点1相关代码：≥ 90%
- 核心模块（digest, acc）：≥ 80%
- 整体项目：≥ 70%
